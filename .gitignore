node_modules/
dist/
.astro/
.DS_Store
.env
.env.*
!.env.example
data/.cache/
coverage/
.mavis
.claude/settings.local.json
.claude/settings.local.json.tmp
.tmp-test/
.codex/
.opencode/
.idea/
.vscode/
*.log
*.tsbuildinfo
*.svelte-kit/
# Integration smoke artifacts — `grove new` scaffolds land here
# if the test mis-routes. Keeping them out of git.
test-tmp/
test-tmp-*/


# Local Grove state — `grove run` writes timestamped scratch projects
# under <monorepo>/.grove/run/, and the release script writes a
# `.release-in-progress` lock file at the repo root. None of this
# should be committed. (Audit finding #10.)
.grove/
.worktrees/
.release-in-progress

# Local scaffold smoke tests — never commit a `grove new` scratch site.
examples/*/
.audit/

# Biome linter/formatter cache
.cache/

# Build-time copy of packages/registry/default/, materialized by
# scripts/build-cli.mjs so `tsc` and the published tarball can pick
# it up. Regenerated on every CLI build — never hand-edit or commit.
packages/cli/src/registry-snapshot/