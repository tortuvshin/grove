---
title: Starlight Components
description: Preview the built-in Starlight components with Lucode Starlight styling.
---

import {
    Aside,
    Badge,
    Card,
    CardGrid,
    Code,
    FileTree,
    Icon,
    LinkButton,
    LinkCard,
    Steps,
    TabItem,
    Tabs,
} from '@astrojs/starlight/components';

export const themeSnippet = `@layer lucode {
  :root[data-theme='dark'] {
    --background: oklch(14.5% 0 0);
    --foreground: oklch(98.5% 0 0);
    --code-background: oklch(20.5% 0 0);
  }
}`;

Use this page as a regression checklist when changing tokens or component overrides. It keeps the
long tail of Starlight UI in one place.

## Cards

<CardGrid>
    <Card title="Docs navigation" icon="open-book">
        Cards use compact type, quiet surfaces, and icon backgrounds that rotate through the
        Starlight accent palette.
    </Card>
    <Card title="Theme tokens" icon="setting">
        Cards, code blocks, tabs, and file trees share the `--code-background` surface token.
    </Card>
    <Card title="Search ready" icon="magnifier">
        The theme keeps hover states restrained so dense docs pages remain easy to scan.
    </Card>
</CardGrid>

## Link Cards

<CardGrid>
    <LinkCard
        title="Getting Started"
        href="guides/getting-started/"
        description="Install the theme, add the plugin, and extend the docs schema."
    />
    <LinkCard
        title="Customize the Theme"
        href="guides/theming/"
        description="Tune color modes, typography, layout dimensions, and component surfaces."
    />
    <LinkCard
        title="Plugin API"
        href="reference/plugin-api/"
        description="Review plugin options, frontmatter extensions, and package exports."
    />
</CardGrid>

## Asides

<Aside>
    Default asides work well for neutral notes that should be visible without becoming the visual
    center of the page.
</Aside>

<Aside type="tip" title="Theme QA" icon="star">
    Keep this page open while adjusting tokens. The fastest way to catch a mismatch is to see every
    state together.
</Aside>

<Aside type="caution" title="Contrast check">
    Review light and dark mode after changing `--foreground`, `--muted-foreground`, `--border`, or
    `--code-background`.
</Aside>

<Aside type="danger">
    Do not judge a docs theme only by headings and paragraphs. Tabs, file trees, badges, and code
    frames need the same care.
</Aside>

## Badges

- <Badge text="Default" />
- <Badge text="Note" variant="note" />
- <Badge text="Success" variant="success" />
- <Badge text="Tip" variant="tip" />
- <Badge text="Caution" variant="caution" />
- <Badge text="Danger" variant="danger" />
- <Badge text="Small" size="small" />
- <Badge text="Medium" size="medium" />
- <Badge text="Large" size="large" />

## Code

<Code
    code={themeSnippet}
    lang="css"
    title="src/styles/global.css"
    mark={['--code-background', '--foreground']}
/>

## File Tree

<FileTree>
    - docs - src - content - docs - guides - getting-started.md - configuration.md - theming.md -
    showcase - **starlight-components.mdx** - splash-pages.mdx - typography.md - astro.config.mjs -
    packages - lucode-starlight - components - custom - overrides - styles - base.css - theme.css
</FileTree>

## Icons

<p>
    <Icon name="starlight" label="Starlight logo" />{' '}
    <Icon name="star" color="goldenrod" size="1.25rem" />{' '}
    <Icon name="rocket" color="var(--foreground)" size="1.25rem" />{' '}
    <Icon name="moon" color="var(--muted-foreground)" size="1.25rem" />{' '}
    <Icon name="right-arrow" color="var(--foreground)" size="1.25rem" />
</p>

## Link Buttons

<LinkButton href="guides/getting-started/">Get Started</LinkButton>
<LinkButton href="guides/theming/" variant="secondary">
    Customize
</LinkButton>
<LinkButton
    href="reference/components/"
    variant="minimal"
    icon="right-arrow"
>
    Theme Components
</LinkButton>
<LinkButton
    href="https://starlight.astro.build/components/using-components/"
    variant="secondary"
    icon="external"
    iconPlacement="start"
>
    Official Component Docs
</LinkButton>

## Steps

<Steps>
1. ### Install the plugin

    Add `lucode-starlight` to Starlight's `plugins` array.

2.  ### Extend the schema

    Add `ExtendDocsSchema` if you want typed splash hero layouts and announcements.

3.  ### Tune the tokens

    Override CSS custom properties from your app stylesheet.

4.  ### Inspect the showcase

    Review this page and the typography page before shipping a custom palette.

</Steps>

## Tabs

<Tabs syncKey="package-manager">
    <TabItem label="bun">```bash bun add lucode-starlight bun run docs ```</TabItem>
    <TabItem label="npm">```bash npm install lucode-starlight npm run dev ```</TabItem>
    <TabItem label="pnpm">```bash pnpm add lucode-starlight pnpm dev ```</TabItem>
</Tabs>

<Tabs syncKey="package-manager">
    <TabItem label="bun">
        Synced tabs keep repeated install, dev, and build examples aligned for readers.
    </TabItem>
    <TabItem label="npm">
        The same tab stays selected here, which is useful on setup-heavy docs pages.
    </TabItem>
    <TabItem label="pnpm">The theme does not require a specific package manager.</TabItem>
</Tabs>
