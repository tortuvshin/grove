/**
 * @grove-dev/svelte — SvelteKit framework adapter for Grove.
 *
 * Same shape as `@grove-dev/astro` and `@grove-dev/nextjs`: a
 * thin wrapper of components, layouts, tokens, and a default
 * template on top of the framework-agnostic core and ui packages.
 *
 * Components are imported by path:
 *   import ItemCard from "@grove-dev/svelte/components/ItemCard.svelte";
 *   import BaseLayout from "@grove-dev/svelte/layouts/BaseLayout.svelte";
 */
export * from "@grove-dev/ui";
