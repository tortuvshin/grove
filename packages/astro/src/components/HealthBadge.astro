---
import type { HealthStatus } from "@grove/core";

interface Props {
  status?: HealthStatus;
}

const { status = "unknown" } = Astro.props;
const label = status.replace(/_/g, " ");
---

<span class={`oc-health oc-health-${status}`}>{label}</span>
