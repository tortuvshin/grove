---
// Grove hero: restrained teal light, one plain headline, one paragraph,
// three pill buttons. No terminals, no panels — the product explains itself.
---

<section
  id="hero-section"
  class="relative overflow-hidden"
  aria-labelledby="hero-title"
>
  <div class="hero-beams" aria-hidden="true"></div>

  <div class="relative mx-auto flex max-w-4xl flex-col items-center px-6 pb-24 pt-16 text-center sm:pb-28 sm:pt-20">
    <a
      href="/roadmap/"
      class="inline-flex items-center gap-2 rounded-full border border-border bg-card/70 py-1.5 pl-3 pr-4 text-xs font-medium text-muted backdrop-blur transition-colors hover:border-border-strong hover:text-fg"
    >
      <span class="rounded-full bg-brand/20 px-2 py-0.5 text-[11px] font-semibold text-brand-light">v0.5.0-next</span>
      grove import turns awesome lists into records
      <svg class="size-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M5 12h14"></path>
        <path d="m12 5 7 7-7 7"></path>
      </svg>
    </a>

    <div class="hero-mark relative mt-12" aria-hidden="true">
      <div class="hero-mark-glow absolute left-1/2 top-1/2 size-40 -translate-x-1/2 -translate-y-1/2"></div>
      <svg class="relative size-20 text-fg sm:size-24" viewBox="0 0 32 32" fill="none">
        <path d="M16 28V13" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
        <path d="M16 13C16 13 16.5 6 24 5C24 5 24 12 16 13Z" fill="currentColor" />
        <path d="M16 13C16 13 15.5 6 8 5C8 5 8 12 16 13Z" fill="currentColor" fill-opacity="0.7" />
        <path d="M16 18C16 18 16.2 21 19.5 22" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
        <path d="M16 21C16 21 15.8 24 12.5 25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
      </svg>
    </div>

    <h1
      id="hero-title"
      class="mt-10 max-w-3xl text-balance text-5xl font-bold leading-[1.08] tracking-tight text-fg sm:text-6xl md:text-[68px]"
    >
      Build directories <span class="text-gradient">that stay useful.</span>
    </h1>
    <p class="mt-6 max-w-2xl text-pretty text-lg leading-relaxed text-muted sm:text-xl">
      Grove is an open-source framework for curated directories, resource hubs, and knowledge sites — searchable, easy to publish, and practical to maintain for years.
    </p>

    <div class="mt-9 flex flex-wrap items-center justify-center gap-3">
      <a
        href="/getting-started/create-a-space/"
        class="rounded-full bg-brand px-6 py-2.5 text-sm font-semibold text-bg transition-colors hover:bg-brand-light"
      >
        Get started
      </a>
      <a
        href="/introduction/"
        class="rounded-full bg-card px-6 py-2.5 text-sm font-semibold text-fg transition-colors hover:bg-card-hover"
      >
        Why Grove?
      </a>
      <a
        href="https://github.com/tortuvshin/grove"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 rounded-full bg-card px-6 py-2.5 text-sm font-semibold text-fg transition-colors hover:bg-card-hover"
      >
        <svg class="size-4" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12Z"></path>
        </svg>
        GitHub
      </a>
    </div>

    <p class="mt-8 text-sm text-subtle">No database · No CMS · MIT licensed</p>
  </div>
</section>

<style>
  .hero-mark {
    animation: hero-mark-float 6s ease-in-out infinite alternate;
  }

  @keyframes hero-mark-float {
    from { transform: translateY(0); }
    to { transform: translateY(-10px); }
  }

  .hero-mark-glow {
    background: radial-gradient(circle, rgb(39 183 200 / 0.3), rgb(125 211 216 / 0.12) 55%, transparent 72%);
    filter: blur(28px);
    animation: hero-mark-pulse 5s ease-in-out infinite alternate;
  }

  @keyframes hero-mark-pulse {
    from { opacity: 0.7; transform: translate(-50%, -50%) scale(0.95); }
    to { opacity: 1; transform: translate(-50%, -50%) scale(1.1); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-mark,
    .hero-mark-glow {
      animation: none;
    }
  }
</style>

<script is:inline>
  (() => {
    // Beam field follows the pointer, desktop only.
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    if (!window.matchMedia('(pointer: fine)').matches) return;
    const hero = document.getElementById('hero-section');
    if (!hero) return;
    hero.addEventListener('pointermove', (event) => {
      const rect = hero.getBoundingClientRect();
      hero.style.setProperty('--mx', `${(((event.clientX - rect.left) / rect.width) * 100).toFixed(1)}%`);
      hero.style.setProperty('--my', `${(((event.clientY - rect.top) / rect.height) * 100).toFixed(1)}%`);
    });
  })();
</script>
