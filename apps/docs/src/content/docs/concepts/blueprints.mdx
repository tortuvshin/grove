---
title: Three blueprints
description: Three schemas, one default scaffold, no init-time choice.
---

import { Aside, Badge, Card, CardGrid, LinkButton, LinkCard, Tabs, TabItem } from '@astrojs/starlight/components';

# Three blueprints

<Aside type="note" title="What a blueprint is">
  A blueprint binds a Grove site to a single record `kind` — `project`, `resource`, or `entity` — and the schema, defaults, and labels that go with it. The blueprint is set in `grove.config.ts` (`blueprint: "project-directory"` by default) and is never picked at init time. `grove init` always scaffolds the `project-directory` flavor.
</Aside>

Grove has three blueprints — `project-directory`, `resource-hub`, `ecosystem-map` — each binding a site to a single record kind.

<CardGrid stagger>
  <Card title="project-directory" icon="seti:notebook">
    Record kind: <Badge text="project" variant="default" size="small" />

    Default scaffold contents: the shipped scaffold at `apps/example/` — open-source tools, agents, libraries, infrastructure.
  </Card>
  <Card title="resource-hub" icon="seti:default">
    Record kind: <Badge text="resource" variant="note" size="small" />

    Default scaffold contents: articles, guides, podcasts, videos, papers.
  </Card>
  <Card title="ecosystem-map" icon="list-format">
    Record kind: <Badge text="entity" variant="tip" size="small" />

    Default scaffold contents: companies, communities, research labs, agencies, people.
  </Card>
</CardGrid>

## Why no init-time picker

`grove init` copies the same canonical scaffold regardless of the blueprint. Switching blueprints happens by editing `grove.config.ts` and re-validating records against the new schema. This keeps the scaffold itself a small, well-tested surface instead of multiplying it three ways.

The plan is not to add more blueprints or a "choose your blueprint" picker. The three current blueprints cover the shape of structured knowledge Grove targets today. New shapes should re-use the existing fields or fork the framework.

## Switching blueprints

```ts title="grove.config.ts"
// grove.config.ts — change this to switch.
export default defineConfig({
  blueprint: "resource-hub",
  // ...
});
```

After switching, `grove check` will report every record that fails the new schema. There are two paths from there:

1. **Rewrite the record** to fit the new schema.
2. **Split the record** into multiple records (an organization + a tool + a contributor are usually three entities, not one).

Switching is rare and intentional. Most sites pick one blueprint and stay.

## What the schema looks like per blueprint

Every record kind shares the [resource base](/concepts/records/) — slug, description, summary, sourceDescription, category, tags, links, content, source, curation, scores, visibility — and adds kind-specific fields:

<Tabs syncKey="kind">
  <TabItem label="project" icon="seti:notebook">
    ```yaml title="data/records/my-project.yml"
    kind: project
    name: my-project
    description: One-line summary.
    repoUrl: https://github.com/me/my-project
    stacks: [typescript]
    platforms: [web]
    licenses: [mit]
    bestFor: ["Self-hosting", "Experiments"]
    whyListed: ["Maintainable", "Documented"]
    ```

    Project-specific fields: `name`, `projectType`, `stack`, `stacks`, `platforms`, `licenses`, `difficulty`, `codebaseSize`, `repoUrl`, `logoUrl`, `screenshots`, `bestFor`, `whyListed`, `caveats`, `distribution.channels`, `github.*`, `health.*`. Full reference at [Record schema](/reference/record-schema/).
  </TabItem>
  <TabItem label="resource" icon="seti:default">
    ```yaml title="data/records/my-resource.yml"
    kind: resource
    title: "A guide to writing knowledge bases"
    type: guide
    topic: knowledge-management
    related: [another-resource-slug]
    ```

    Resource-specific fields: `title`, `type` (one of `guide`, `comparison`, `link`, `explainer`, `tool`, `video`, `article`, `course`, `book`, `podcast`, `other`), `topic`, `related`, `publishedAt`, `author`.
  </TabItem>
  <TabItem label="entity" icon="list-format">
    ```yaml title="data/records/grove-foundation.yml"
    kind: entity
    name: Grove Foundation
    type: organization
    founded: "2026-01-01"
    location: "Ulaanbaatar, MN"
    ```

    Entity-specific fields: `name`, `type` (one of `company`, `organization`, `community`, `school`, `university`, `research-lab`, `agency`, `service`, `product`, `person`, `other`), `founded`, `location`, `members`, `parent`.
  </TabItem>
</Tabs>

## What blueprint controls

The blueprint primarily changes **the shape of new records** — what fields are required, what enums are accepted, and what `kind` discriminator value is mandatory. The blueprint also chooses sensible defaults for:

- The default slug collection (`projects` / `resources` / `entities` — overridable via `routes.directory`).
- The default display labels (`project` / `projects` — overridable via `labels`).

What it does **not** control:

- The site shell, navigation, components, layouts, theme — those come from your Astro project.
- The build pipeline — every blueprint runs through the same `prepareDirectory()` + Astro build.

## Which blueprint should I pick?

<Aside type="tip" title="Decision rule of thumb">
  Pick the blueprint whose **record `kind`** matches the entity you keep wanting to add. If most rows are "things you can install or fork", pick `project-directory`. If most rows are "things you read", pick `resource-hub`. If most rows are "organizations or people in a space", pick `ecosystem-map`. Mixing kinds under one blueprint is not supported — switch the blueprint instead.
</Aside>

<CardGrid>
  <LinkCard
    title="Curating tools / agents / libraries?"
    description="Each row is a runnable thing with a repo and a stack. Use project-directory."
    href="/concepts/blueprints/#project-project-directory"
  />
  <LinkCard
    title="Curating articles / guides / podcasts?"
    description="Each row is something to read, watch, or listen to. Use resource-hub."
    href="/concepts/blueprints/#resource-resource-hub"
  />
  <LinkCard
    title="Mapping orgs / people / communities?"
    description="Each row is an actor in a space, with founding dates and members. Use ecosystem-map."
    href="/concepts/blueprints/#entity-ecosystem-map"
  />
</CardGrid>

## A worked example

A directory of open-source AI tools uses `project-directory`:

```yaml title="data/records/ollama.yml"
# data/records/ollama.yml
kind: project
name: Ollama
description: Get up and running with large language models locally.
repoUrl: https://github.com/ollama/ollama
stacks: [go]
platforms: [macos, linux, windows]
licenses: [mit]
bestFor: ["Local LLM runtime", "GGUF model support"]
whyListed: ["Easy install", "Active releases", "Good docs"]
```

A research-collection site uses `resource-hub`:

```yaml title="data/records/attention-is-all-you-need.yml"
# data/records/attention-is-all-you-need.yml
kind: resource
title: "Attention Is All You Need"
type: paper
topic: transformers
author: Vaswani et al.
publishedAt: "2017-06-12"
```

An ecosystem map of the Mongolian open-source community uses `ecosystem-map`:

```yaml title="data/records/grove-foundation.yml"
# data/records/grove-foundation.yml
kind: entity
name: Grove Foundation
type: organization
founded: "2026-01-01"
location: "Ulaanbaatar, MN"
```

Same Grove. Same build. Different `kind`, different schema, different lenses.

## Next steps

<div style={{ display: 'flex', gap: '0.75rem', flexWrap: 'wrap', marginTop: '1rem' }}>
  <LinkButton href="/reference/config/" variant="primary" icon="right-arrow">
    Configure
  </LinkButton>
  <LinkButton href="https://github.com/withgrove/docs" variant="minimal" icon="external">
    GitHub
  </LinkButton>
</div>

<CardGrid>
  <LinkCard
    title="Reference: record schema"
    description="Every field, every kind."
    href="/reference/record-schema/"
  />
  <LinkCard
    title="Reference: grove.config.ts"
    description="blueprint, routes, labels."
    href="/reference/config/"
  />
  <LinkCard
    title="Author a record"
    description="How to write YAML and long-form content for any blueprint."
    href="/content/author-a-record/"
  />
  <LinkCard
    title="Concept: records"
    description="The shared resource base every kind extends."
    href="/concepts/records/"
  />
</CardGrid>
