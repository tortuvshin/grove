---
title: Quickstart
description: Get a Grove site running in under ten minutes.
---

import { Aside, CardGrid, LinkButton, LinkCard, Steps } from '@astrojs/starlight/components';

The fastest path to a working Grove site — five commands, minimal explanation. For the full walkthrough, with what each step does and why, see [Getting started](/getting-started/scaffold/).

## Prerequisites

<Aside type="note" title="Before you begin">
  - Node `>=22.12.0`
  - pnpm `10.12.1` (or any compatible Node package manager — swap `pnpm`/`pnpm dlx` below for `npm`/`npx` or `bun`/`bunx`)
</Aside>

<Steps>

1. ## Scaffold a space

   ```bash
   pnpm dlx @grove-dev/cli@latest init my-space
   cd my-space
   ```

2. ## Start the dev server

   ```bash
   pnpm dev
   ```

   Open `http://localhost:4321` — the site renders with six sample records.

3. ## Replace the sample record

   Edit or replace a file under `data/records/`:

   ```yaml title="data/records/my-project.yml"
   kind: project
   name: my-project
   description: One-line summary of what my-project does.
   repoUrl: https://github.com/me/my-project
   stacks: [typescript]
   licenses: [mit]
   ```

   See [Add your first project](/getting-started/first-record/) for the full field reference.

4. ## Validate and build

   ```bash
   pnpm exec grove check
   pnpm build
   ```

   The deployable site is in `dist/`. Any static host serves it.

5. ## Deploy

   ```bash
   pnpm build && pnpm preview
   ```

   Preview the production build locally, then follow the [Deploy your site →](/deployment/overview/) guide — it covers GitHub Pages, Cloudflare, Netlify, and self-hosted setups.

</Steps>

## Next

<CardGrid>
  <LinkCard
    title="Add taxonomy"
    description="Add a taxonomy entry and use it as a category on a record."
    href="/content/taxonomy-files/"
  />
  <LinkCard
    title="Create a collection"
    description="Add a data/collections/<slug>.yml or run `grove collection promote`."
    href="/concepts/collections/"
  />
  <LinkCard
    title="Enable GitHub sync"
    description="Each record grows a `github.*` block automatically."
    href="/automation/sync-github/"
  />
  <LinkCard
    title="Custom OG artwork"
    description="Edit `public/og-image.svg`; the sentinel keeps your version."
    href="/customize/branding/"
  />
</CardGrid>

<div style={{ display: 'flex', gap: '0.75rem', flexWrap: 'wrap', marginTop: '1rem' }}>
  <LinkButton href="/getting-started/scaffold/" variant="primary" icon="rocket">
    Get started
  </LinkButton>
  <LinkButton href="https://github.com/tortuvshin/grove" variant="minimal" icon="external">
    GitHub
  </LinkButton>
</div>
