# Open Curated basic example

This example mirrors the project created by:

```sh
open-curated init examples/basic
```

The Astro/Tailwind UI lives in `packages/astro/template/default`. This folder is a generated consumer project with sample `data/*.yml` files so the CLI, core schemas, and Astro template can be tested together.
